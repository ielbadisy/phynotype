% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_clusters.R
\name{plot_cluster_network}
\alias{plot_cluster_network}
\title{Plot a network graph of cluster relationships}
\usage{
plot_cluster_network(
  x,
  data = NULL,
  embedding = c("auto", "pca", "famd", "mca", "mds"),
  min_similarity = 0,
  ...
)
}
\arguments{
\item{x}{A \code{cluster_fit}, \code{metacluster_fit}, or \code{cluster_explore} object.}

\item{data}{Optional numeric matrix, data frame, or distance object used to
compute the embedding. Defaults to the training data stored in \code{x}.}

\item{embedding}{Embedding method. \code{"auto"} selects PCA for numeric data,
FAMD for mixed numeric/categorical data, MCA for categorical data, and
classical MDS for distance inputs. \code{"pca"}, \code{"famd"}, \code{"mca"}, and
\code{"mds"} may be selected explicitly.}

\item{min_similarity}{Numeric threshold in \verb{[0, 1]}; edges with similarity
below this value are omitted. Similarity is \code{1} for coincident centroids
and \code{0} for the two most distant centroids in the solution. Defaults to
\code{0} (draw every edge).}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Summarize a clustering solution as a network: one node per cluster,
positioned at its centroid in the two-dimensional embedding and sized by
cluster size, connected by edges whose thickness reflects how similar
(close) each pair of cluster centroids is. This gives a compact overview
of how many well-separated groups exist versus how many clusters sit close
together, which is easy to miss on a dense point-cloud scatter plot.
}
\examples{
fit <- cluster(iris[, 1:4], method = "kmeans", k = 3, seed = 1)
plot_cluster_network(fit)
}
\seealso{
\code{\link[=explore]{explore()}} for the underlying embedding, \code{\link[=plot_clusters]{plot_clusters()}},
\code{\link[=plot_coassoc]{plot_coassoc()}} for the observation-level co-association network.
}
