% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lime_explain.R
\name{lime_explain}
\alias{lime_explain}
\title{LIME-style local explanations for clustering}
\usage{
lime_explain(
  object,
  new_data,
  n_features = 5L,
  n_permutations = 500L,
  kernel_width = NULL,
  target = c("cluster", "score"),
  cluster = NULL,
  seed = NULL,
  parallel = FALSE,
  cores = NULL,
  workers = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{cluster_fit} object with prediction support.}

\item{new_data}{Row-by-feature data containing observations to explain.}

\item{n_features}{Maximum number of local effects returned per observation.}

\item{n_permutations}{Number of perturbed neighborhood samples per
observation.}

\item{kernel_width}{Positive numeric kernel width. If \code{NULL}, uses
\code{0.75 * sqrt(p)}, where \code{p} is the number of features.}

\item{target}{Output target. \code{"cluster"} explains a one-vs-cluster local
assignment indicator; \code{"score"} explains a cluster-specific prediction
score.}

\item{cluster}{Optional cluster to explain. If \code{NULL}, each observation is
explained for its baseline predicted cluster.}

\item{seed}{Optional integer random seed.}

\item{parallel}{Logical; if \code{TRUE}, use \code{functionals::fmap()} when the
suggested \code{functionals} package is installed.}

\item{cores}{Optional positive integer number of cores passed to
\code{functionals::fmap()} when \code{parallel = TRUE}.}

\item{workers}{Optional number of workers passed to \code{functionals::fmap()}.
Deprecated alias for \code{cores}.}

\item{progress}{Logical; if \code{TRUE}, request progress reporting from
\code{functionals::fmap()}.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{lime_explanation} object with tidy \code{explanations} and
\code{neighborhoods} data frames.
}
\description{
Explain individual cluster assignments or cluster scores with a locally
weighted surrogate model. The method builds a synthetic neighborhood around
each observation, predicts the fitted clustering behavior in that
neighborhood, weights perturbed samples by proximity to the explained
observation, and fits a simple weighted linear model.
}
\details{
For an observation \eqn{x}, perturbed samples \eqn{z_i}, fitted clustering
rule \eqn{f}, target extractor \eqn{g}, local surrogate \eqn{h}, and kernel
\eqn{\pi_x(z_i)}, the LIME objective is

\deqn{
  \hat{h}
  =
  \arg\min_{h \in H}
  \sum_{i=1}^{N}
  \pi_x(z_i)
  \left[g\{f(z_i)\} - h(z_i)\right]^2
  + \Omega(h).
}

phynotype uses a weighted linear surrogate for \eqn{H}. Feature ranking is
based on the absolute fitted local coefficients after standardizing features
by the training-data scale. The penalty term \eqn{\Omega(h)} is represented
operationally by returning the largest \code{n_features} effects, which keeps the
explanation sparse and readable without adding a heavy modeling dependency.

For \code{target = "cluster"}, \eqn{g\{f(z_i)\}} is an indicator that the
predicted cluster equals the cluster being explained. For \code{target = "score"},
it is the cluster-specific membership or similarity score.
}
\examples{
fit <- cluster(iris[, 1:4], method = "kmeans", k = 3, seed = 1)
lx <- lime_explain(fit, iris[1:2, 1:4], n_permutations = 50, seed = 1)
lx
}
