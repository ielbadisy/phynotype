% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_importance.R
\name{feature_importance}
\alias{feature_importance}
\title{Permutation feature importance for clustering}
\usage{
feature_importance(
  object,
  data = NULL,
  features = NULL,
  metric = c("instability", "silhouette", "total_within"),
  loss = NULL,
  n_repeats = 10L,
  seed = NULL,
  parallel = FALSE,
  cores = NULL,
  workers = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{cluster_fit} object with prediction support.}

\item{data}{Optional row-by-feature data used for evaluation. Defaults to the
training data stored in \code{object}.}

\item{features}{Optional character vector or numeric column index specifying
features to evaluate.}

\item{metric}{Built-in importance metric. \code{"instability"} measures the
fraction of changed cluster assignments. \code{"silhouette"} measures loss of
mean silhouette width. \code{"total_within"} measures increase in within-cluster
dispersion.}

\item{loss}{Optional custom loss function with signature
\code{loss(data, clusters, object)}.}

\item{n_repeats}{Number of independent permutations per feature.}

\item{seed}{Optional integer random seed.}

\item{parallel}{Logical; if \code{TRUE}, use \code{functionals::fmap()} when the
suggested \code{functionals} package is installed.}

\item{cores}{Optional positive integer number of cores passed to
\code{functionals::fmap()} when \code{parallel = TRUE}.}

\item{workers}{Optional number of workers passed to \code{functionals::fmap()}.
Deprecated alias for \code{cores}.}

\item{progress}{Logical; if \code{TRUE}, request progress reporting from
\code{functionals::fmap()}.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{feature_importance} object with components:
\describe{
\item{\code{results}}{Data frame with one row per (feature, repeat) combination
recording the baseline loss, the permuted loss, and the computed
importance.}
\item{\code{summary}}{Data frame with one row per feature, containing the mean
importance, standard error, and number of repeats.}
}
}
\description{
Estimate global feature importance by measuring how much a fitted clustering
solution changes when each feature is independently permuted. The method is
model-agnostic: it uses the fitted object's \code{predict()} method and therefore
applies to clustering engines with native prediction support.
}
\details{
Let \eqn{f} denote the fitted clustering rule, \eqn{X} the evaluation data,
\eqn{\hat{c}=f(X)} the baseline predicted partition, and
\eqn{X^{(m)}_{\pi(j)}} the data matrix where feature \eqn{j} has been
permuted in repetition \eqn{m}. For the default instability metric, feature
importance is

\deqn{
  FI_j = \frac{1}{M}\sum_{m=1}^{M}
  \frac{1}{n}\sum_{i=1}^{n}
  I\{\hat{c}_i \neq f(X^{(m)}_{\pi(j)})_i\}.
}

This quantity estimates the expected fraction of assignments that change
when the marginal information in feature \eqn{j} is broken. Larger values
indicate that the fitted clustering rule relies more strongly on that
feature.

For score-style internal metrics, the package computes a baseline score
\eqn{S(f, X)} and a permuted score
\eqn{S(f, X^{(m)}_{\pi(j)})}. For silhouette, larger is better and

\deqn{
  FI_j = \frac{1}{M}\sum_{m=1}^{M}
  \left[S(f, X) - S(f, X^{(m)}_{\pi(j)})\right].
}

For total within-cluster sum of squares, smaller is better and

\deqn{
  FI_j = \frac{1}{M}\sum_{m=1}^{M}
  \left[W(f, X^{(m)}_{\pi(j)}) - W(f, X)\right].
}

Users can pass a custom \code{loss} function for domain-specific objectives. It
must accept \verb{(data, clusters, object)} and return a numeric scalar where
larger values mean worse fit.
}
\examples{
fit <- cluster(iris[, 1:4], method = "kmeans", k = 3, seed = 1)
imp <- feature_importance(fit, n_repeats = 3, seed = 1)
imp$summary
}
\references{
Breiman, L. (2001). Random forests. \emph{Machine Learning}, \strong{45}(1), 5--32.
(Permutation importance concept introduced in the context of random forests.)

Fisher, A., Rudin, C. and Dominici, F. (2019). All models are wrong, but
many are useful: Learning a variable's importance by studying an entire class
of prediction models simultaneously. \emph{Journal of Machine Learning Research},
\strong{20}(177), 1--81.
}
\seealso{
\code{\link[=lime_explain]{lime_explain()}} for local explanations, \code{\link[=ceteris_paribus]{ceteris_paribus()}} for
individual conditional profiles, \code{\link[=predict.cluster_fit]{predict.cluster_fit()}} which is used
internally.
}
